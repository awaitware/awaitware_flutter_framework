name: awaitware_flutter_framework_example
description: "Playground app for awaitware_flutter_framework — a running demo of the library's chainable extension methods."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  awaitware_flutter_framework:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
