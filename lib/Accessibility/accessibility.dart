/// Tailwind-inspired accessibility utilities for Flutter.
///
/// Import this single file for every accessibility extension — the 1 doc
/// page listed below has a corresponding file.
///
/// ## No-op stub (Flutter has no backing API at all — documented, not faked)
/// - forced-color-adjust → `forced_color_adjust_extensions.dart` — a
///   browser forced-colors-mode (Windows High Contrast Mode) concept
///   specific to DOM content; Flutter draws its own pixels and isn't
///   automatically recolored by the OS the way a browser tab is
library;

export 'forced_color_adjust_extensions.dart';
