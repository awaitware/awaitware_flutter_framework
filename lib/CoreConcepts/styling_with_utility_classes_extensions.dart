/// Tailwind's utility-first methodology.
///
/// Docs: https://tailwindcss.com/docs/styling-with-utility-classes
///
/// **Nothing to implement here — this page describes the approach this
/// entire project already is.** "Style by composing small, single-purpose
/// utility classes directly, instead of writing custom CSS" is exactly what
/// every `.p4().bgBlue500().roundedLg()`-style chain across all 15
/// preceding folders does, just as Dart extension methods instead of CSS
/// classes. There's no separate mechanism this page introduces beyond that.
library;
