import 'package:flutter/widgets.dart';

/// Tailwind CSS-inspired `backdrop-filter: grayscale()` utilities for Flutter.
///
/// Docs: https://tailwindcss.com/docs/backdrop-filter-grayscale
///
/// **No-op.** Same reason as `backdrop-filter-brightness` — see
/// `backdrop_filter_extensions.dart`. These methods exist only so this doc
/// page has a corresponding file; they return the widget unchanged.
extension BackdropFilterGrayscaleExtensions on Widget {
  Widget backdropGrayscale0() => this;
  Widget backdropGrayscale() => this;
}
