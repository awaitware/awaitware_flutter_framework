import 'package:flutter/widgets.dart';

/// Tailwind CSS-inspired `backdrop-filter: invert()` utilities for Flutter.
///
/// Docs: https://tailwindcss.com/docs/backdrop-filter-invert
///
/// **No-op.** Same reason as `backdrop-filter-brightness` — see
/// `backdrop_filter_extensions.dart`. These methods exist only so this doc
/// page has a corresponding file; they return the widget unchanged.
extension BackdropFilterInvertExtensions on Widget {
  Widget backdropInvert0() => this;
  Widget backdropInvert() => this;
}
