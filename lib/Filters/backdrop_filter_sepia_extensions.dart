import 'package:flutter/widgets.dart';

/// Tailwind CSS-inspired `backdrop-filter: sepia()` utilities for Flutter.
///
/// Docs: https://tailwindcss.com/docs/backdrop-filter-sepia
///
/// **No-op.** Same reason as `backdrop-filter-brightness` — see
/// `backdrop_filter_extensions.dart`. These methods exist only so this doc
/// page has a corresponding file; they return the widget unchanged.
extension BackdropFilterSepiaExtensions on Widget {
  Widget backdropSepia0() => this;
  Widget backdropSepia() => this;
}
